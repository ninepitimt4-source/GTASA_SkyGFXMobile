name: AML Mod Compiler

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout Current Repo
        uses: actions/checkout@v4

      - name: 2. Force Clean Everything
        run: |
          # ลบโฟลเดอร์เจ้าปัญหาทิ้งให้หมดแบบถอนรากถอนโคน
          rm -rf structs
          rm -rf mod
          rm -rf libs
          rm -rf obj

      - name: 3. Setup AML Template
        run: |
          wget -q https://github.com/RusJJ/AndroidModLoader/archive/refs/heads/main.zip -O AML_src.zip
          unzip -q AML_src.zip
          mv AndroidModLoader-main/mod ./mod
          rm -rf AndroidModLoader-main AML_src.zip

      - name: 4. Get SAUtils
        uses: actions/checkout@v4
        with:
          repository: RusJJ/SAUtils
          path: mod/SAUtils

      - name: 5. Get Structs
        uses: actions/checkout@v4
        with:
          repository: RusJJ/GTASAAndroid_Structs
          path: structs

      - name: 6. Install NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25c

      - name: 7. Build Mod
        run: |
          ndk-build NDK_PROJECT_PATH=. -j$(nproc)

      - name: 8. Upload Finished Mod
        uses: actions/upload-artifact@v4
        with:
          name: SkyGfx-Android-32bit
          path: libs/
          
